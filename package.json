{
  "private": true,
  "scripts": {
    "build": "pulp build -- --strict --censor-lib"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.0"
  }
}
