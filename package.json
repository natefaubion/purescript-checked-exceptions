{
  "private": true,
  "scripts": {
    "build": "pulp build -- --strict --censor-lib"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.5.0"
  }
}
